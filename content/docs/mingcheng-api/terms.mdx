---
title: 服务条款
icon: FileText
description: MingChengAPI 服务条款与使用协议
---

# 铭诚 API 隐私政策与服务使用条款  

**生效日期：** 2026 年 9 月 6 日  
**服务域名：** api.b4qaq.cn  
**运营主体：** MingChengNetworkStudio（铭诚网络工作室）  

欢迎使用铭诚 API（以下简称"本服务"、"MingChengAPI"或"我们"）。本服务是为第三方开发者应用提供技术支撑的后端接口服务。  

本文件由隐私政策与服务使用条款两部分组成，二者不可分割。使用本服务即表示您（以下或称"开发者"）已充分阅读、理解并同意遵守本文件的全部内容。如您不同意任一内容，请立即停止使用本服务。

---

## 第一部分：隐私政策

本部分阐述了我们在处理个人信息过程中的原则与实践。鉴于本服务的技术特性，我们需要明确界定数据处理活动中的角色与责任。

### 1. 角色定位与适用范围

#### 1.1 角色界定

本服务在个人信息处理活动中扮演受托处理者的角色：

- **个人信息处理者：** 指集成本服务的第三方开发者应用（如"永昼天气 Eternal"、"腕上信驿 WearPost"）。开发者负责向其最终用户（下称"用户"）收集个人信息，并承担《中华人民共和国个人信息保护法》规定的处理者责任。
- **受托处理者：** 指 MingChengAPI。我们仅依据开发者的指令（API 调用）代为处理数据，不独立决定处理目的与方式。
- **个人信息主体：** 指开发者应用的最终用户。

#### 1.2 适用范围

本隐私政策适用于本服务在处理以下业务场景数据时的所有行为：

- 设备验证、激活与设备信息查询接口；
- 天气数据查询接口；
- 邮件代理收发接口；
- AI 模型中转与 AI 任务接口；
- 设备隧道（局域网反向代理）接口；
- 支付回调、系统公告及其他系统接口。

#### 1.3 沟通渠道

本服务不直接面向最终用户提供用户界面。最终用户关于个人信息的权利主张（查阅、复制、更正、删除等），应通过开发者应用的官方渠道提出，由开发者应用依法响应。如涉及需本服务配合的技术操作，开发者应用可联系我们进行协同处理。

### 2. 个人信息的收集与处理

我们仅在响应 API 调用请求时，为完成特定业务功能而处理必要信息。绝不超范围收集。

#### 2.1 天气服务

- **收集字段：** 地理位置信息（由设备 GNSS 或基站生成的经纬度坐标）、设备标识符（设备型号、唯一 ID）、系统版本、应用版本、请求源 IP。
- **处理目的：** 依据经纬度坐标向数据源查询对应位置的气象数据；依据设备信息进行服务激活验证、频次控制及异常流量分析。
- **处理逻辑：** 经纬度坐标仅用于当次请求的数据源查询，不进行持久化存储。设备信息经加密后存储于服务器端，用于生成统计报表。

#### 2.2 邮件服务

**收集字段：**

- **账户凭证：** 邮箱地址、IMAP/SMTP 服务器地址与端口、授权凭证（OAuth Token 或授权码）。
- **邮件元数据：** 收件人地址、邮件主题、收发时间戳、邮件大小、附件文件名。
- **邮件内容：** 邮件正文、附件数据流。
- **设备信息：** 设备型号、系统版本、网络制式。

**处理目的：** 代理开发者应用连接用户指定的邮件服务商（如 QQ 邮箱、163 邮箱、Gmail、Outlook 等），完成邮件的收取、发送、列表同步及已发送箱归档。

**处理逻辑：**

- **账户凭证：** 仅存储于内存中，用于建立与邮件服务商的连接。我们郑重承诺：不在数据库或文件系统中对用户的邮箱账户凭证进行持久化存储。当连接断开、开发者发起删除指令或内存释放时，相关凭证立即销毁。
- **邮件内容：** 仅作为数据转发的中继通道。邮件正文与附件在内存中进行流式处理，不进行持久化存储，不落盘，不记录全文日志。仅在内存中保留极短时间以确保网络传输的可靠性，传输完成后立即释放。
- **邮件元数据：** 用于构建 API 返回列表，部分字段（如收件人、主题）可能在传输链路日志中短暂出现，但我们会进行脱敏处理，且日志保留时间不超过 7 天。
- **设备信息：** 加密存储，用于服务统计与激活验证。

#### 2.3 AI 模型中转与 AI 任务

- **收集字段：** 设备 APIKey（用于鉴权）、请求中的消息内容（messages / prompt）、工具调用（function calling）参数、所选模型名（别名）。
- **处理目的：** 将请求转换并转发至相应的第三方大模型供应商（如 OpenAI、Anthropic 等）完成推理，并按模型配置计费。
- **处理逻辑：** 消息内容仅用于当次请求的转发与流式回传，我们不持久化保存对话内容；同步模型中转按 token 后置结算，AI 任务（轮询）与设备隧道接口不扣本网关配额（noQuota，上游凭证由请求体携带或设备自身提供）。

#### 2.4 设备隧道

- **收集字段：** 设备名、设备内网 IP、网关下发的 6 位设备码、连接与最近在线时间、转发请求的方法 / 路径 / 请求头等元数据。
- **处理目的：** 在网关程序与服务端之间建立多路复用 WebSocket 反向隧道，并按调用方请求流式转发至设备本机或其所在局域网内的目标主机。
- **处理逻辑：** 服务端仅做实时寻址与字节流转发，不持久化转发的请求体 / 响应体内容；调用方鉴权用的 Key 字段在转发给设备前自动剔除。

#### 2.5 运营与安全日志

我们自动收集访问日志，包括请求时间、请求接口、HTTP 状态码、耗时、User-Agent 等。

- 此类日志用于排查系统故障、防御网络攻击（如 DDoS、暴力破解）及性能优化。
- 日志中的敏感字段（如 Authorization 头、具体邮件内容）会被自动过滤或替换。

#### 2.6 我们不予主动收集的信息

- 最终用户的真实姓名、电话号码、身份证号码等身份识别信息（除非此类信息作为邮件内容的一部分由开发者主动传输，但我们不主动提取）。
- 用户在开发者应用内的操作行为轨迹（如按钮点击流），除非该操作触发了 API 调用。

### 3. 数据存储与安全保障

我们采取了符合行业标准的技术与管理措施，以保护个人信息安全。

#### 3.1 存储架构

- **存储地域：** 所有生产环境服务器均位于中华人民共和国大陆境内。
- **存储期限：**
  - 邮件相关数据：遵循"零持久化"原则，仅内存驻留，随连接生命周期结束而消失。
  - 设备与账户配置数据：加密存储，保留期限与开发者的应用生命周期一致，开发者删除应用或注销账号后立即匿名化或删除。
  - 运行日志：保留不超过 90 天，超期自动清理。
- **存储介质：** 采用具备冗余备份的分布式存储系统，防止单点故障导致的数据丢失。

#### 3.2 技术防护

- **传输加密：** 全站强制启用 HTTPS/TLS 1.3 加密传输，杜绝中间人攻击。
- **存储加密：** 敏感配置信息（如加密密钥、内部凭证）采用 AES-256 算法进行字段级加密存储。
- **访问控制：** 实行严格的 RBAC（基于角色的访问控制）权限管理体系。仅限必要的运维人员在授权范围内访问后台系统，且所有操作均有审计日志。
- **网络隔离：** 核心数据库部署于内网环境，不直接暴露公网访问端口。
- **日志脱敏：** 自动化日志系统会对敏感字段进行掩码处理（如将 1231231313 显示为 1231\*\*\*\*\*1313）。

#### 3.3 安全事件响应

尽管我们采取了严密的安全措施，但互联网环境不存在绝对的安全。如发生个人信息泄露等安全事件，我们将按照法律法规要求，及时通过邮件或应用内公告向开发者及受影响用户告知事件基本情况、影响范围及我们已采取的补救措施。

### 4. 信息共享、转让与披露

我们深知个人信息的重要性，不会随意共享、转让或披露您的信息，但以下情形除外：

#### 4.1 为实现功能所必需的共享

- **邮件服务：** 为完成邮件投递，我们必须将收件人地址、邮件主题与正文通过 SMTP 协议转发至目标邮件服务商的服务器。此为电子邮件协议的固有环节，无法避免。
- **天气服务：** 为查询天气数据，我们必须将经纬度坐标发送至上游天气数据源（如第三方气象服务商）。
- **AI 服务：** 为获取模型推理结果，我们必须将消息内容（prompt / messages）转发至对应的第三方大模型供应商（如 OpenAI、Anthropic 等）。
- **设备隧道：** 为完成透传，我们必须将请求经 WebSocket 转发至开发者的网关设备，或其指定的局域网目标主机（X-Tunnel-Target）。

#### 4.2 法律法规要求的披露

根据法律法规、司法裁决、政府主管部门（如网信办、公安机关）的强制性要求，我们需要提供相关信息的。

#### 4.3 保护权益所必需的披露

为维护国家安全、公共利益，或保护本服务、开发者、用户的生命财产安全及其他合法权益，在必要时披露的。

#### 4.4 委托处理

我们可能委托专业的云服务提供商（如 IDC 机房、CDN 加速服务商）提供基础设施支持。这些受托方仅能接触到提供服务所必需的最少数据，且受到严格的保密协议约束，我们无法控制其内部管理行为，但会选择信誉良好的合作伙伴。

#### 4.5 聚合信息

我们可能对设备信息进行去标识化处理，生成汇总的统计图表（如"昨日活跃设备型号分布"），此类信息无法识别特定个人，不属于个人信息范畴，不在本限制之内。

### 5. 您的权利

#### 5.1 开发者的权利

作为直接签约方，开发者拥有对其在本服务中注册账户的管理权：

- **访问与更正权：** 开发者可通过管理后台或 API 查询和更新其应用配置信息。
- **删除权：** 开发者可删除其创建的应用或设备。删除指令生效后，相关联的配置数据将从活跃数据库中移除。
- **数据导出权：** 开发者可申请导出与其应用相关的非敏感配置数据。

#### 5.2 最终用户的权利

最终用户的权利由开发者应用负责落实。本服务作为受托方，将配合开发者应用完成以下操作：

- 当开发者应用收到用户的删除邮件或注销账号请求时，我们将配合停止处理相关凭证，并清除内存中的缓存数据。
- 当开发者应用收到用户的查阅请求时，我们将配合提供存储于我方的非敏感元数据（如请求日志）。

#### 5.3 投诉与联系

如您对本隐私政策有任何疑问、意见或建议，可通过以下渠道联系我们：

- **邮箱：** studio@b4qaq.cn / 525148989@qq.com
- **网站：** https://api.b4qaq.cn

我们会在收到反馈后的 15 个工作日内予以回复。

### 6. 儿童隐私保护

本服务主要面向开发者及成年用户。我们不直接向 14 周岁以下的儿童提供服务。如果我们发现自己在未获监护人同意的情况下收集了儿童的个人信息，将立即采取措施删除相关数据。如果开发者应用面向儿童提供服务，开发者有义务确保已获得监护人的明示同意，并采取相应的保护措施。

### 7. 隐私政策的更新

随着法律法规的变化及服务功能的调整，我们可能会适时修订本隐私政策。

- **更新发布：** 更新后的版本将通过 api.b4qaq.cn 官网公告及邮件通知（如适用）的方式发布。
- **生效：** 重大变更将在公告后 7 天生效。非重大变更（如联系方式更新）即时生效。
- **异议处理：** 政策更新后，如您继续使用本服务，即视为您已充分阅读并同意更新后的内容。如您不同意更新后的条款，应立即停止使用并注销账户。

---

## 第二部分：服务使用条款

本部分规定了开发者在使用本服务过程中的权利与义务，以及服务边界与责任限制。

### 1. 账户注册与管理

#### 1.1 注册资格

开发者承诺在注册时年满 14 周岁，具备完全的民事权利能力和民事行为能力，能够独立承担法律责任。如代表公司或其他组织注册，需保证已获得相应的授权。

#### 1.2 信息真实性

开发者注册时提供的邮箱地址、应用信息等应当真实、准确、完整，并在信息发生变更时及时更新。因信息不实导致的服务中断或法律责任，由开发者自行承担。

#### 1.3 凭证安全

开发者将获得 API Key、Secret 或 Token 等调用凭证。开发者必须：

- 妥善保管上述凭证，严禁将其泄露给任何第三方（包括终端用户）。
- 严禁在客户端代码（如 Web 前端 JS、移动端 App 二进制文件）中硬编码明文凭证。
- 如发现凭证泄露或被盗用，应立即通过管理后台重置或联系我们冻结账户。

我们对因开发者保管不善导致的凭证泄露及产生的后果不承担责任。

#### 1.4 账户类型与权限

本服务设有不同的账户角色（如 User、Dev、ROOT），不同角色拥有不同的系统权限。开发者不得利用权限漏洞尝试越权访问系统资源。

### 2. 服务内容与使用规范

#### 2.1 服务内容

本服务提供以下核心接口能力：

- **设备认证与管理：** 验证设备合法性，管理设备白名单/黑名单。
- **邮件代理服务：** 提供 IMAP 邮件拉取、SMTP 邮件发送、文件夹管理等代理能力。
- **天气数据服务：** 提供基于地理位置的天气信息查询能力。
- **AI 模型中转与任务：** 提供 OpenAI / Anthropic 双协议兼容的模型中继、按 token 计费，以及兼容弱网设备的异步 AI 任务。
- **设备隧道：** 提供多路复用流式 WebSocket 反向隧道，使调用方可通过标准 HTTP 访问网关设备本机或其局域网内主机。
- **系统公告：** 提供系统级与应用级公告查询。
- **支付与订阅集成：** 对接爱发电等第三方支付平台，验证订阅状态。

#### 2.2 正当使用

开发者承诺：

- 严格按照本条款及官方 API 文档的规范调用接口。
- 仅将邮件服务用于合法的邮件收发，不得利用本服务发送垃圾邮件（Spam）、钓鱼邮件、诈骗信息、暴力恐怖信息或任何违反法律法规的邮件内容。
- 仅将天气服务用于正常的天气查询，不得利用接口进行恶意爬虫、数据窃取或超出合理频率的压测。
- 仅将 AI 服务用于合法的模型调用，不得利用其生成、传播违反法律法规或公序良俗的内容，不得尝试绕过模型供应商的安全策略或进行未授权的批量滥用。
- 仅将设备隧道用于访问自有或已获授权的设备 / 主机，不得利用隧道从事内网渗透、未授权访问等活动，并应自行确保被转发目标的安全。
- 在其应用的隐私政策中明确告知用户数据将传输至 api.b4qaq.cn 进行处理，并已获得用户的合法有效授权。

#### 2.3 禁止行为

开发者不得使用本服务从事以下活动：

- **违法犯罪：** 利用本服务传播违法信息、侵犯他人知识产权（如盗版书籍、软件）、隐私权或名誉权。
- **网络攻击：** 尝试对 api.b4qaq.cn 进行 DDoS 攻击、SQL 注入、暴力破解、端口扫描或任何形式的网络探测。
- **系统干扰：** 尝试逆向工程、反编译、反汇编服务端程序；尝试绕过频次限制；尝试干扰服务的正常运行或破坏数据完整性。
- **资源滥用：** 恶意占用大量系统资源（如建立大量无效连接、发送超大附件耗尽带宽）；利用本服务作为文件存储服务器（非邮件附件用途）。
- **虚假身份：** 冒充他人或机构进行 API 调用；使用虚假信息注册账户。

#### 2.4 邮件内容责任

开发者对其通过本服务发送和接收的邮件内容负全部责任。本服务仅提供传输管道，不对邮件内容的合法性、准确性、完整性做任何明示或暗示的担保，也不对邮件内容进行事前审查。如因邮件内容侵权或违法导致法律责任，由开发者自行承担。

### 3. 知识产权

#### 3.1 我们的权利

api.b4qaq.cn 域名、LOGO、API 文档、服务端源代码、界面设计、架构设计等一切与本服务相关的智力成果，其知识产权归 MingChengNetworkStudio 所有或已获得合法授权。开发者仅获得使用权，不得进行复制、修改、传播或用于商业开发（除非获得书面授权）。

#### 3.2 开发者的权利

开发者通过本服务创建的应用、上传的配置信息，其知识产权归开发者所有。开发者通过邮件服务传输的数据，其所有权归开发者或其用户所有。

#### 3.3 授权范围

我们授予开发者一项有限的、非独占的、不可转让的、可撤销的许可，允许开发者仅在其集成了本服务的应用中调用 API 接口。任何超出此范围的使用均构成侵权。

### 4. 服务可用性、变更与终止

#### 4.1 服务可用性

我们致力于提供 7x24 小时的不间断服务，但受限于软硬件维护、升级、网络运营商故障、不可抗力（如自然灾害、战争、政府管制）等因素，服务可能会出现临时中断。

对于计划内的维护，我们将尽量提前在官网发布公告。

> **特别说明：** 邮件服务的稳定性高度依赖于上游邮件服务商（QQ、163、Gmail、Outlook 等）的策略。因上游服务限流（如 SMTP 发送频率限制）、接口变更、服务宕机或网络链路抖动导致的邮件延迟、丢失或无法发送，属于邮件协议固有风险，本服务不承担赔偿责任。

#### 4.2 服务变更

我们保留在不违反法律法规的前提下，随时修改、暂停或终止部分或全部服务的权利。对于重大变更（如停止某项核心功能），我们将提前 30 天发布公告。

#### 4.3 账户终止

**开发者主动终止：** 开发者可随时停止使用服务并申请注销账户。注销后，相关数据将按照隐私政策的规定进行处理。

**我们主动终止：** 如发生以下情况，我们有权不经通知暂停或终止开发者的账户：

- 违反本使用条款的任何规定。
- 长期未活跃（如超过 180 天无任何 API 调用）。
- 提供虚假注册信息。
- 从事危害本服务安全或损害他人利益的行为。
- 未按时支付应付费用（如涉及付费服务）。

#### 4.4 终止后的效力

账户终止后，开发者的 API 调用权限立即失效，本服务不再处理相关请求。本条款中涉及保密、知识产权、责任限制、争议解决的条款在终止后仍然有效。

### 5. 费用与支付

#### 5.1 免费服务

本服务可能提供一定的免费调用额度。免费服务不保证 SLA（服务等级协议），且可能包含速率限制。

#### 5.2 付费服务

如涉及付费订阅（如通过爱发电平台）：

- 资费标准以购买页面或第三方平台公示为准。
- 订阅费用一经支付，除服务无法交付等特殊情况外，概不退还。
- 开发者应自行负责处理与其最终用户之间的费用结算问题，本服务不参与开发者与其用户之间的财务往来。

#### 5.3 欠费处理

如开发者账户产生欠费，我们将暂停其 API 调用权限，直至费用结清。欠费超过一定期限，我们有权删除相关数据。

### 6. 免责声明与责任限制

#### 6.1 服务"按现状"提供

本服务及其包含的所有内容（包括 API 接口、文档、数据）均按"现状"和"现有功能"基础提供，不作任何明示或暗示的保证，包括但不限于对适销性、特定用途适用性、非侵权性或无误性、不间断性的保证。

#### 6.2 技术局限性

开发者理解并确认：

- 互联网环境存在固有风险，包括数据安全、网络中断、黑客攻击等，尽管我们尽力防护，但不能完全排除风险。
- 基于 Xiaomi Vela 等嵌入式操作系统的特性，后台同步可能因系统资源回收策略而中断，这属于设备端的限制，非本服务可控范围。
- 邮件传输依赖复杂的互联网路由，可能存在延迟、丢件或被误判为垃圾邮件的风险。

#### 6.3 责任限制

在法律允许的最大范围内，对于因使用或无法使用本服务而产生的任何直接、间接、附带、特殊、惩戒性或后果性损害（包括但不限于利润损失、数据丢失、业务中断），无论是否基于合同、侵权（包括 negligence）或其他法律理论，即使我们已被告知存在此类损害的可能性，我们均不承担责任。

#### 6.4 责任上限

在任何情况下，我们对您承担的全部责任总额，不超过您在导致责任的事件发生前的十二（12）个月内实际支付给我们的服务费用总额。如您未支付任何费用，则责任上限为一百元人民币（¥100.00）。

#### 6.5 例外情况

上述责任限制不适用于因我们故意不当行为、重大过失、欺诈或法律明确规定不得排除的责任（如人身伤害）。

### 7. 争议解决与适用法律

#### 7.1 适用法律

本条款的解释、效力及纠纷解决，均适用中华人民共和国法律（不包括冲突法规则）。

#### 7.2 友好协商

因本条款引起的或与本条款有关的任何争议，双方应首先通过友好协商解决。协商不成的，任何一方均有权向本服务运营主体所在地（即 MingChengNetworkStudio 所在地）有管辖权的人民法院提起诉讼。

### 8. 其他条款

#### 8.1 完整协议

本文件（含隐私政策与使用条款）构成双方关于本服务使用的完整协议，取代双方此前就此事项达成的所有口头或书面的意向或协议。

#### 8.2 独立性

如果本条款中的任何一条被有管辖权的法院认定为无效或不可执行，不影响其他条款的效力，其余条款仍应有效并具有约束力。

#### 8.3 弃权

我们未行使或延迟行使本条款项下的任何权利，不构成对该权利的放弃。单项或部分行使某项权利，亦不妨碍我们进一步行使该权利或行使其他权利。

#### 8.4 转让

未经我们事先书面同意，开发者不得将其在本条款项下的权利义务转让给任何第三方。我们可在通知开发者后，将本条款项下的权利义务转让给关联公司或继任实体。

#### 8.5 通知

我们向您发出的通知（如政策更新、服务变更），可通过官网公告、电子邮件或应用内推送等方式进行。通知一经发出即视为送达。

---

感谢您阅读本文件。再次感谢您对铭诚 API 的信任与支持！

© 2026 MingChengNetworkStudio. All Rights Reserved.
